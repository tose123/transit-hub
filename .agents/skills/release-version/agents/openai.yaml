interface:
  display_name: "发布版本"
  short_description: "发布当前 fork 的 TransitHub 时间戳版本与 GHCR 镜像"
  default_prompt: "使用 $release-version 为当前 fork 的 TransitHub 自动生成中国标准时间版本 tag，同步版本与 GHCR 发布引用，完成验证，并在逐项确认后发布分支、tag、GitHub Release 和 GHCR 镜像。"
